name: Build with Docker
description: Build application with Docker
runs:
  steps:
    - run: docker build . --file src/main/docker/Dockerfile.jvm --tag academy:$(date +%s) && docker images
      shell: bash
